# Production QA

Production deploy is intentionally shipped as a self-contained atomic HTML bundle so CSS, JavaScript, photographs, and venue artwork cannot be omitted by a partial Vercel deployment.

The source design remains in `index.html`, `styles.css` / `base.css`, `script.js`, and `assets/`.

QA fix: August 29, 2026.
